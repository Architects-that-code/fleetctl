Key directories/files:

config/fleet.yaml.tmpl (a template for the user to base their config on, or just documentation)
internal/app (contains the main application logic)
internal/client (OCI client wrapper)
internal/config (config parsing)
internal/fleet (fleet management logic)
internal/util (helpers)
cmd/fleetctl/main.go (entry point)
docs/ (optional: documentation)